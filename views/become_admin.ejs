<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %></title>
    <link rel='icon' href='/images/courtney.png' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/forms.css' />
</head>
<body>
    <div class="container">
    <%- include('header'); -%>
    <% if(!user.admin) { %>
        <form>
            <h2><%= title %></h2>
            <div>
                <label for="passcode"></label>
                <input id="passcode" type="password" name="passcode" required>

                <% if(passcodeError) { %>
                    <p><%= passcodeError %></p>
                    <button type="submit">Submit Passcode</button>
                <% } %>
            </div>
        </form>
    <% } else { %>
            <div>You're already an admin! Thanks for helping lead this revolution.</div>
            <div>
                <img src="/images/army.avif" style="width: 100%;">
            </div>
        <% } %>
        <%- include('footer'); -%>
    </div>
</body>
</html>